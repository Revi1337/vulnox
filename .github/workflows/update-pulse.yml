name: Update Vulnerability Data

permissions:
  contents: write

on:
  schedule:
    - cron: '0 */2 * * *' # Every 2 hours
  workflow_dispatch: # Allow manual trigger

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Fetch Latest Pulse & Analytics
        run: |
          node scripts/fetch-pulse.mjs
          node scripts/analyze-trends.mjs

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git checkout package-lock.json || echo "No package-lock.json changes to discard"
          git add data/*.json public/data/*.json
          git commit -m "chore: update live pulse and analytics data [skip ci]" || echo "No changes to commit"
          git pull --rebase origin main
          git push
